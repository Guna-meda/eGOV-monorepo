{
  "name": "egov-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "complaint-service",
    "frontend",
    "gis-service"
  ],
  "scripts": {
    "db:generate": "npm --workspace @egov/shared run db:generate",
    "db:migrate": "npm --workspace @egov/shared run db:migrate"
  }
}
